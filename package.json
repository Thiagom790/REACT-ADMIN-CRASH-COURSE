{
  "name": "react-admin-test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "server": "json-server --watch db.json --middlewares ./range.js --port 5000",
    "client": "cd ./client && yarn start",
    "dev": "concurrently \"yarn server\" \"yarn client\""
  },
  "dependencies": {
    "concurrently": "^7.0.0",
    "json-server": "^0.17.0"
  }
}
